export const months_active_labels = Array.from({ length: 12 }, (_, i) => 1+i);
export const daysOfWeek_active_labels = ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
export const hours_active_labels = Array.from({ length: 24 }, (_, i) => i);